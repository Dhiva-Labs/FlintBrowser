#!/bin/sh
set -e

case "$1" in
    configure)
        # Chromium's SUID sandbox helper must be setuid root.
        if [ -f /opt/flint-browser/chrome-sandbox ]; then
            chmod 4755 /opt/flint-browser/chrome-sandbox || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
